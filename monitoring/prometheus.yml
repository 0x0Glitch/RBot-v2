global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: morpho-v2-reallocator
    metrics_path: /metrics
    scheme: http
    static_configs:
      - targets: [host.docker.internal:9090]
        labels:
          service: morpho-v2-reallocator
